#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{

	window(1,1,80,25);
	textbackground(BLUE);
	textcolor(WHITE);
	int i;
	char ch;
	clrscr();
	for(i = 2; i < 80; i++)
	{
		//col = 1;
		gotoxy(i,1);
		putch(205);
		//col = 25;
		gotoxy(i,24);
		putch(205);
	}

     /*	for(i = 2; i < 24; i++)
	{
		gotoxy(1,i);
		putch(186);
		gotoxy(80,i);
		putch(186);

	}
	gotoxy(1,1);
	putch(201);
	gotoxy(80,1);
	putch(187);
	gotoxy(1,24);
	putch(200);
	gotoxy(80,24);
	putch(188);    */
}